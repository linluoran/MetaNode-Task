syntax = "proto3";

package protobuf;
option go_package="./protobuf";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Usercenter_rpc {
  rpc Ping(Request) returns(Response);
}
