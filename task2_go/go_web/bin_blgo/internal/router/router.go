package router

//
//func InitRouter() {
//	r := gin.Default()
//	r.Use(middleware.CORS()) // 全局中间件
//	apiGroup := r.Group("/api/v1")
//	RegisterUserRoutes(apiGroup)  // 注册用户模块路由
//	RegisterOrderRoutes(apiGroup) // 注册订单模块路由
//}
